#!/bin/bash

p hdparm

# Read test (safe to data) (tests block device not filesystem)

sudo hdparm -tT /dev/sdXX

# or (tests filesystem speeds)

  # Create a file named 'ass' = 10GB   "Choose either zero or urandom"
    
    # Write test
    dd if=/dev/zero of=/mnt/Original/ass bs=1G count=10
    
	
	# Read test
	dd if=/mnt/Original/ass of=/dev/null
	
	
