#!/bin/bash
# set the percentage of reserved blocks on the partition /dev/sdf1 to 0%:


sudo tune2fs -m 0 /dev/sdf1


# Be sure to replace /dev/sdf1 with your own path
# you can use the

df

# command to get a listing of your filesystem