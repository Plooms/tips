#!/bin/bash
# Remove Metadata from video files



# To remove all metadata and insert new title
ffmpeg -i "$file" -map_metadata -1 -c copy -metadata title="$name" "$out"


# To insert new title only
ffmpeg -i "$file" -c copy -metadata title="$name" "$out"
