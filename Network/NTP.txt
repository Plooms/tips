#!/bin/bash
# Install and configure NTP (Network Time Protocol)

  
  
  # Install 
    p ntp

  
  # Edit 
  n /etc/ntp.conf
  
  # Make sure the following commands exist in /etc/ntp.conf (iburst is optional)
  
  server 0.pool.ntp.org iburst
  server 1.pool.ntp.org iburst
  server 2.pool.ntp.org iburst
  server 3.pool.ntp.org iburst
  
  
  # Enable & Start NTP
  
sudo systemctl enable ntpd.service

sudo systemctl restart ntpd.service ;sudo systemctl status ntpd.service



#######################################################################
	
# Resources

https://wiki.archlinux.org/index.php/Ntp
